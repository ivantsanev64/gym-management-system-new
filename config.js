const FEATURE_FLAGS = {
  AI_RECOMMENDATIONS: true,
  FACE_RECOGNITION: false,
  PAYMENT_GATEWAY: false,
  DARK_MODE: true
};

// Usage in code
if (FEATURE_FLAGS.AI_RECOMMENDATIONS) {
  showRecommendedClasses();
}